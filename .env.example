# Database URL for PostgreSQL connection (required for production)
# This will be automatically set by Railway when linking a PostgreSQL service
DATABASE_URL=postgres://user:password@host.railway.app:5432/database

# Environment setting (development or production)
NODE_ENV=development

# Port for the server (optional, Railway sets this automatically)
PORT=5000

# Set to true to force the application to exit when database connection fails
# This can help with debugging Railway deployment issues
# FORCE_EXIT_ON_DB_FAILURE=true
