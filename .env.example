# Environment setting (development or production)
NODE_ENV=development

# Port for the server (optional, Railway sets this automatically)
PORT=5000

# SQLite database configuration
# The database will be stored in the ./data directory
# No external database connection is required!

# Persistence settings
# Set to true if you want to use in-memory storage only (no persistence)
# USE_MEMORY_STORAGE=false
